name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Build Docker image
      run: docker build -t weather-frontend:dev .

    - name: Log in to Azure Container Registry
      uses: azure/docker-login@v1
      with:
        login-server: devops9nestcandidate.azurecr.io
        username: ${{ secrets.devops9nestcandidate }}
        password: ${{ secrets.3y06K395LOQQ1TywBf1JV0VUicrgIbdbCq/8ZPw83y+ACRDG3yfh }}

    - name: Push Docker image
      run: docker push devops9nestcandidate.azurecr.io/weather-frontend:dev
